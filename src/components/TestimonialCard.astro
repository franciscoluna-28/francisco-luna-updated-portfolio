---
const { title, comment, authorName, authorRole, authorImage } = Astro.props;
---

<figure
  class="flex flex-col justify-center p-8 text-center border border-mainGray/20 hover:border-main-500 duration-200 hover:shadow-main-400 shadow-sm md:p-12 rounded-lg"
>
  <blockquote class="mx-auto mb-8 max-w-2xl text-gray-400 dark:text-gray-400">
    <h3 class="text-lg font-semibold text-white">
      {title}
    </h3>
    <p class="my-4 text-left">
      {comment}
    </p>
  </blockquote>
  <figcaption class="flex items-center space-x-3">
    <img class="w-9 h-9 rounded-full bg-cover object-cover"  src={authorImage} alt={authorName} />
    <div class="space-y-0.5 font-medium text-white text-left">
      <div>{authorName}</div>
      <div class="text-sm font-normal text-gray-400">{authorRole}</div>
    </div>
  </figcaption>
</figure>
